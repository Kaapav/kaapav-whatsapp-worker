{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "kaapav-chat-ui/dist"
  "routes": [
    { "src": "/$", "dest": "/index.html" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
